export type Message = {
  role: string;
  content: string;
};

export type ImageData = {
  content: string;
};

export type profileData = {
  image: string;
  activeImage: string;
  name: string;
  active: boolean;
};
